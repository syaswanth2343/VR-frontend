# Railway → Frontend service → Variables (available at *build* time for Vite).
# Use your real backend public URL; no trailing slash after "api".
#
# VITE_API_BASE=https://vr-backend-production.up.railway.app/api
